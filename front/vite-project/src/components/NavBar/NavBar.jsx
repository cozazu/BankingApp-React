const NavBar = () => {
    return (
        <div className="{styles.navbarContainer}">
            <div>
                <span>HOME</span>
                <span>MIS TURNOS</span>
                <span>ABOUT</span>
                <span>CONTACTO</span>
            </div>
        </div>
    );
};

export default NavBar;